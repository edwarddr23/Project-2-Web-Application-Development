doctype html
html
    head 
        title Project 2
        link(rel='stylesheet', type='text/css', href="https://cdn.jsdelivr.net/npm/water.css@2/out/light.css")
    body
        if !hide_signin
            if user 
                span 
                    span #{user.f_name} #{user.l_name} #{' '}
                    a(href="/logout") Logout
            else
                a(href="/login") Sign in
        block content
