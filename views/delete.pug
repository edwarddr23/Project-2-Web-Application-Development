extends layout.pug 

block content 
    h1 Delete Contact
    h2 #{contact.f_name} #{contact.l_name}
    p Are you sure you want to delete this contact?
    form(action="/" + contact.id + "/delete" method="post")
        button(type="submit") Yes! 
        a(href="/" + contact.id) Nevermind